FROM lscr.io/linuxserver/code-server:latest

USER root

# 1. Install critical system tools and native Node.js/npm directly from Ubuntu repos
RUN apt-get update && apt-get install -y zsh curl git psmisc tmux python3 python3-pip nodejs npm && \
    rm -rf /var/lib/apt/lists/*

# 2. Force root user layer context workspace rules
USER root
WORKDIR /workspace

# 3. Install valid Node packages globally as root
RUN npm install -g pnpm pm2 @anthropic-ai/claude-code

# 4. Install python applications globally as root to bypass /config permission blocks
RUN pip3 install aider-chat --break-system-packages

# 5. Apply default shell choices natively and restore standard abc runtime execution user
RUN usermod -s /bin/zsh abc

USER abc
