services:
  # =========================================================================
  # 1. EDGE PROXY & INFRASTRUCTURE TIER
  # =========================================================================
  docker-socket-proxy:
    image: tecnativa/docker-socket-proxy
    container_name: docker-socket-proxy
    environment:
      - DOCKER_API_VERSION=1.44
      - CONTAINERS=1
      - NETWORKS=1
      - SERVICES=1
      - SWARM=1
      - TASKS=1
      - VOLUMES=1
      - INFO=1
      - VERSION=1
      - EVENTS=1
      - PING=1
      - IMAGES=1
      - LOGS=1
      - EXEC=1
    volumes:
      - ./haproxy.cfg.template:/usr/local/etc/haproxy/haproxy.cfg.template:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    networks:
      - traefik_public

  traefik:
    image: traefik:v3.2
    container_name: traefik
    depends_on:
      - docker-socket-proxy
    environment:
      - DOCKER_HOST=tcp://docker-socket-proxy:2375
    command:
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.endpoint=tcp://docker-socket-proxy:2375"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.letsencrypt.acme.email=conar@sous.tools"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      - "--providers.file.filename=/etc/traefik/dynamic.yml"
      # - "--log.level=INFO"
      - "--log.level=DEBUG"

    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "letsencrypt:/letsencrypt"
      - "./traefik_dynamic.yml:/etc/traefik/dynamic.yml:ro"

    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - traefik_public

    deploy:
      resources:
        limits:
          memory: 512m

  # =========================================================================
  # 2. DATA STORAGE & QUEUE TIER
  # =========================================================================
  redis:
    image: redis:alpine
    container_name: redis
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    ports:
      - "6379:6379"
    volumes:
      - ./.data/redis:/data
    restart: unless-stopped
    networks:
      - traefik_public
    deploy:
      resources:
        limits:
          memory: 512m

  neo4j:
    image: neo4j:5-community
    container_name: neo4j
    restart: unless-stopped
    ports:
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/sousToolsPassword
      - NEO4J_server_memory_pagecache_size=1G
      - NEO4J_server_memory_heap_initial__size=1G
      - NEO4J_server_memory_heap_max__size=2G
    volumes:
      - ./.data/neo4j:/data
    networks:
      - traefik_public

    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.neo4j-secure.rule=Host(`graph.sous.tools`)"
      - "traefik.http.routers.neo4j-secure.entrypoints=websecure"
      - "traefik.http.routers.neo4j-secure.tls=true"
      - "traefik.http.routers.neo4j-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.neo4j-secure.service=neo4j-service"
      - "traefik.http.routers.neo4j.rule=Host(`graph.localhost`)"
      - "traefik.http.routers.neo4j.entrypoints=web"
      - "traefik.http.routers.neo4j.service=neo4j-service"
      - "traefik.http.services.neo4j-service.loadbalancer.server.port=7474"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 3g

  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./.data/qdrant:/qdrant/storage
    networks:
      - traefik_public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.qdrant-secure.rule=Host(`qdrant.sous.tools`)"
      - "traefik.http.routers.qdrant-secure.entrypoints=websecure"
      - "traefik.http.routers.qdrant-secure.tls=true"
      - "traefik.http.routers.qdrant-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.qdrant-secure.service=qdrant-service"
      - "traefik.http.routers.qdrant.rule=Host(`qdrant.localhost`)"
      - "traefik.http.routers.qdrant.entrypoints=web"
      - "traefik.http.routers.qdrant.service=qdrant-service"
      - "traefik.http.services.qdrant-service.loadbalancer.server.port=6333"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 2g

  # =========================================================================
  # 3. LLM GATEWAY & LOCAL MODEL TIER
  # =========================================================================
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    volumes:
      - ./.data/ollama:/root/.ollama
    restart: unless-stopped
    networks:
      - traefik_public
    deploy:
      resources:
        limits:
          memory: 8g

  ollama-pull:
    image: ollama/ollama:latest
    container_name: ollama-pull
    depends_on:
      - ollama
    environment:
      - OLLAMA_HOST=http://ollama:11434
    entrypoint: /bin/sh
    command:
      - "-c"
      - |
        echo "Waiting for Ollama service to respond..."
        sleep 10
        MODELS="qwen2.5-coder:7b deepseek-coder-v2 yi-coder:9b nomic-embed-text llama3.2-vision"
        for model in $$MODELS; do
          echo "Verifying/Pulling model $$model..."
          ollama pull $$model
        done
        echo "Ollama model synchronization complete!"
    networks:
      - traefik_public

  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm
    restart: unless-stopped
    command:
      ["--config", "/app/config.yaml", "--port", "4000", "--detailed_debug"]
    volumes:
      - ./litellm_config.yaml:/app/config.yaml
    environment:
      - LITELLM_MASTER_KEY=sk-1234
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_ENV=${INFISICAL_ENV}
    networks:
      - traefik_public
    depends_on:
      - ollama
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.litellm-secure.rule=Host(`ai.sous.tools`)"
      - "traefik.http.routers.litellm-secure.entrypoints=websecure"
      - "traefik.http.routers.litellm-secure.tls=true"
      - "traefik.http.routers.litellm-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.litellm-secure.service=litellm-service"
      - "traefik.http.routers.litellm.rule=Host(`ai.localhost`)"
      - "traefik.http.routers.litellm.entrypoints=web"
      - "traefik.http.routers.litellm.service=litellm-service"
      - "traefik.http.services.litellm-service.loadbalancer.server.port=4000"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 2g

  # =========================================================================
  # 4. AGENTIC ORCHESTRATION & IDE TIER
  # =========================================================================
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    user: root
    restart: unless-stopped
    environment:
      - N8N_HOST=n8n.sous.tools
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - N8N_COMMAND_ENABLE=true
      - WEBHOOK_URL=https://n8n.sous.tools/
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=false
      - NODES_EXCLUDE=[]
      - N8N_BLOCK_ENV_ACCESS_IN_NODE=false
      - N8N_BLOCK_SVC_EXECUTION_FROM_COMMAND=false
      - N8N_SECURE_COOKIE=false
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_ENV=${INFISICAL_ENV}
    volumes:
      - ./.data/n8n:/root/.n8n
      - ./.data/n8n:/home/node/.n8n
      - ./packages/workflows:/etc/n8n/workflows
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - traefik_public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.n8n-secure.rule=Host(`n8n.sous.tools`)"
      - "traefik.http.routers.n8n-secure.entrypoints=websecure"
      - "traefik.http.routers.n8n-secure.tls=true"
      - "traefik.http.routers.n8n-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.n8n-secure.service=n8n-service"
      - "traefik.http.routers.n8n.rule=Host(`n8n.localhost`)"
      - "traefik.http.routers.n8n.entrypoints=web"
      - "traefik.http.routers.n8n.service=n8n-service"
      - "traefik.http.services.n8n-service.loadbalancer.server.port=5678"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 2g

  editor:
    build:
      context: .
      dockerfile: Dockerfile.editor
    container_name: code-server
    environment:
      - PUID=1001
      - PGID=1001
      - TZ=America/New_York
      - PASSWORD=sousToolsPassword
      - SUDO_PASSWORD=sousToolsPassword
      - PROXY_DOMAIN=*
      - OPENAI_API_BASE=https://ai.sous.tools/v1
      - OPENAI_API_KEY=sk-1234
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_ENV=${INFISICAL_ENV}
    volumes:
      - .:/config/workspace
      - .:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
    networks:
      - traefik_public
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.editor-secure.rule=Host(`editor.sous.tools`)"
      - "traefik.http.routers.editor-secure.entrypoints=websecure"
      - "traefik.http.routers.editor-secure.tls=true"
      - "traefik.http.routers.editor-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.editor-secure.service=editor-service"
      - "traefik.http.routers.editor.rule=Host(`editor.localhost`)"
      - "traefik.http.routers.editor.entrypoints=web"
      - "traefik.http.routers.editor.service=editor-service"
      - "traefik.http.services.editor-service.loadbalancer.server.port=8443"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 4g
          cpus: "2.0"

  mcpo:
    build:
      context: .
      dockerfile: Dockerfile.mcpo
    container_name: mcpo
    restart: unless-stopped
    volumes:
      - ./mcp_config.json:/app/mcp_config.json
      - /var/run/docker.sock:/var/run/docker.sock
      - .:/workspace
    networks:
      - traefik_public
    environment:
      - API_KEY=your-mcp-internal-key
    deploy:
      resources:
        limits:
          memory: 2g

  open-webui:
    image: ghcr.io/open-webui/open-webui:v0.11.0
    container_name: open-webui
    restart: unless-stopped
    networks:
      - traefik_public
    volumes:
      - ./.data/open-webui:/app/backend/data
      - .:/workspace
      - ./packages/config/sanitizer.py:/app/backend/data/functions/sanitizer.py
    environment:
      - OPENAI_API_BASE_URL=http://litellm:4000/v1
      - OPENAI_API_KEY=sk-1234
      - WEBUI_SECRET_KEY=password
      - WEBUI_AUTH=true
      - ENABLE_MCP=true
      - DEFAULT_MODELS=alias/planning
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_ENV=${INFISICAL_ENV}
      # - TOOL_SERVER_CONNECTIONS=http://mcp-filesystem:8000/api?key=your-mcp-internal-key
    depends_on:
      - litellm
      - mcpo
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.open-webui-secure.rule=Host(`webui.sous.tools`)"
      - "traefik.http.routers.open-webui-secure.entrypoints=websecure"
      - "traefik.http.routers.open-webui-secure.tls=true"
      - "traefik.http.routers.open-webui-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.open-webui-secure.service=open-webui-service"
      - "traefik.http.routers.open-webui.rule=Host(`webui.localhost`)"
      - "traefik.http.routers.open-webui.entrypoints=web"
      - "traefik.http.routers.open-webui.service=open-webui-service"
      - "traefik.http.services.open-webui-service.loadbalancer.server.port=8080"
      - "traefik.docker.network=traefik_public"
    deploy:
      resources:
        limits:
          memory: 3g

  openwebui-seeder:
    image: python:3.11-slim
    container_name: openwebui-seeder
    depends_on:
      open-webui:
        condition: service_started
    volumes:
      - ./scripts/seed_openwebui.py:/app/seed_openwebui.py
      - ./.data/open-webui:/app/backend/data
      - .:/workspace
    command: >
      bash -c "pip install requests && python /app/seed_openwebui.py"
    networks:
      - traefik_public

  cptr:
    image: ghcr.io/open-webui/computer:latest
    container_name: cptr
    user: "1001:1001"
    volumes:
      - cptr-data:/data
      - cptr_projects:/projects
      - .:/workspace
    environment:
      - PYTHONUNBUFFERED=1
      - LOG_LEVEL=DEBUG
    restart: unless-stopped
    networks:
      - traefik_public
    labels:
      - "traefik.enable=true"
      # Production Secure Router (Handles Let's Encrypt for public domain only)
      - "traefik.http.routers.cptr-secure.rule=Host(`cptr.sous.tools`)"
      - "traefik.http.routers.cptr-secure.entrypoints=websecure"
      - "traefik.http.routers.cptr-secure.tls=true"
      - "traefik.http.routers.cptr-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.cptr-secure.service=cptr-service"

      # Local Development Router (Plain HTTP for .localhost, bypassing ACME completely)
      - "traefik.http.routers.cptr-local.rule=Host(`cptr.localhost`)"
      - "traefik.http.routers.cptr-local.entrypoints=web"
      - "traefik.http.routers.cptr-local.service=cptr-service"

      # Shared Backend Service Definition
      - "traefik.http.services.cptr-service.loadbalancer.server.port=8000"
      - "traefik.http.services.cptr-service.loadbalancer.server.scheme=http"
      - "traefik.docker.network=traefik_public"

  # =========================================================================
  # 5. PRODUCTION APPLICATION STACK TIER
  # =========================================================================
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: api
    restart: unless-stopped
    depends_on:
      - redis
    environment:
      - INFISICAL_CLIENT_ID=${INFISICAL_CLIENT_ID}
      - INFISICAL_CLIENT_SECRET=${INFISICAL_CLIENT_SECRET}
      - INFISICAL_PROJECT_ID=${INFISICAL_PROJECT_ID}
      - INFISICAL_ENV=${INFISICAL_ENV}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-secure.rule=Host(`api.sous.tools`)"
      - "traefik.http.routers.api-secure.entrypoints=websecure"
      - "traefik.http.routers.api-secure.tls=true"
      - "traefik.http.routers.api-secure.tls.certresolver=letsencrypt"
      - "traefik.http.routers.api-secure.service=api-service"
      - "traefik.http.routers.api.rule=Host(`api.localhost`)"
      - "traefik.http.routers.api.entrypoints=web"
      - "traefik.http.routers.api.service=api-service"
      - "traefik.http.services.api-service.loadbalancer.server.port=3001"
      - "traefik.docker.network=traefik_public"
    networks:
      - traefik_public
    deploy:
      resources:
        limits:
          memory: 1g

volumes:
  letsencrypt:
  cptr-data:
  cptr_projects:

networks:
  traefik_public:
    name: traefik_public
    external: true
