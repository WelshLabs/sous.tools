/**
 * Empty slot for the @modal parallel route when on /tv/[deckId] (full editor).
 * The actual preview modal lives at @modal/tv/[deckId]/preview/page.tsx
 * and is only activated when navigating to /tv/[deckId]/preview.
 */
export default function DeckEditorModalSlot() {
  return null;
}
